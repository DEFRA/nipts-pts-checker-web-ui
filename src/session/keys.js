const pkcecodes = Object.freeze({
  verifier: "verifier",
});

const tokens = Object.freeze({
  accessToken: "accessToken",
  tokenExpiry: "tokenExpiry",
  idToken: "idToken",
  state: "state",
  nonce: "nonce",
  magicPassword: "magicPassword",
  sso: "sso",
});

export default {
  pkcecodes,
  tokens,
};
